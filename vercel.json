{
    "version": 2,
    "builds": [
      {
        "src": "Makefile",
        "use": "@vercel/static-build",
        "config": {
          "buildCommand": "make export",
          "distDir":      "static-site"
        }
      }
    ]
  }
  